{
  "name": "fhe-swap-dapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start:node": "npx hardhat node",
    "deploy:local": "npx hardhat run scripts/deploy-swap.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^3.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
